# The two element lists behind a jigsaw pool, so the mod's village houses can be
# appended to vanilla's pools without replacing them (which would break every
# other mod that adds village buildings).
public net.minecraft.world.level.levelgen.structure.pools.StructureTemplatePool templates
public-f net.minecraft.world.level.levelgen.structure.pools.StructureTemplatePool rawTemplates
